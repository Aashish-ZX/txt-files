#include<stdio.h>
#include<stdlib.h>
int main(){
int queue[]={12,15,18,9,7,59,89,17};
int total = 0;
for(int i=0;i<7;i++){
    printf("\n%d: diff: %d",queue[i],abs(queue[i+1]-queue[i]));

    total += abs(queue[i+1]-queue[i]);
}
printf("\n total :%d",total);
return 1;
}